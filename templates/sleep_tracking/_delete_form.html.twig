<form
    class="{{ form_class|default('d-inline-block') }}"
    method="post"
    action="{{ path('app_sleep_tracking_delete', {'id': sleep_tracking.id}) }}"
    onsubmit="return confirm('{{ confirm_message|default('Delete this sleep tracking entry?')|e('js') }}');"
>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sleep_tracking.id) }}">
    <button class="{{ button_class|default('btn btn-outline-danger') }}">
        <i class="bi bi-{{ button_icon|default('trash') }} me-1"></i>{{ button_label|default('Delete') }}
    </button>
</form>
